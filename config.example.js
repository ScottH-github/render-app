// config.example.js
// Copy this file to config.js and adjust the endpoint to match your setup.
// The default assumes the CORS proxy (workers-proxy) runs on port 8787.
export const COMFYUI_ENDPOINT = "http://localhost:8787";
